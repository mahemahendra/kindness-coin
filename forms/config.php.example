<?php
/**
 * SMTP & Application Configuration — EXAMPLE FILE
 *
 * Copy this file to config.php and fill in your real values.
 * NEVER commit config.php to version control (.gitignore already excludes it).
 *
 * For Hostinger email, use your cPanel email account password.
 */

return [
    'smtp' => [
        'host'       => 'smtp.hostinger.com',
        'port'       => 587,
        'encryption' => 'tls',
        'username'   => 'your-email@yourdomain.com',
        'password'   => 'your-email-password',         // Hostinger cPanel email account password
    ],
    'admin_email'  => 'your-email@yourdomain.com',
    'sender_name'  => 'Kindness Coin',
    'sender_email' => 'your-email@yourdomain.com',
    'db' => [
        'host'     => '127.0.0.1',
        'port'     => 3306,
        'dbname'   => 'kindness',
        'username' => 'kindness_user',          // Use a dedicated DB user, not root
        'password' => 'strong-db-password',
    ],
    'admin_password' => 'change-this-before-going-live',

    // Set to false in production to enable transactional emails.
    'skip_email' => false,

    // Story categories shown on the public Stories page and admin panel.
    'categories' => ['Childhood Cancer', 'Diabetes', 'Disaster Relief', 'Environment', 'Humanitarian', 'Hunger', 'Vision', 'Youth'],
];
